describe('data', () => {
  it('debería exponer una funcion validateEmail', () =>{
    assert.isFunction(validateEmail);
  });
  it('deberia exponer una funcion ', () =>{
    assert.isFunction();
  });
  it('deberia exponer una función ', () =>{
    assert.isFunction();
  });
  it('deberia exponer una función', () =>{
    assert.isFunction();
  });
});
    